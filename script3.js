function myFunction(firstArgument, secondArgument){
	let concatenatedString = firstArgument + " " + secondArgument
	return concatenatedString
}

let results = myFunction("Howdy", "Partner");

console.log(results)